module LindaDrawer exposing (..)

import Html exposing (Html)


{-|

    For this exercise you will create a Drawer component.
    No need to worry about the styling, just the functionality.

    Requirements:
    - It should have a config
        - abillity to set a title
        - abillity to set the content
        - ability to set a button
    - It should have a model
        - it should have a boolean value to determine if the drawer is open or not

-}
